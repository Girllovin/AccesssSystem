package smallITgroup.client.dto;

import lombok.Getter;
import lombok.NoArgsConstructor;

@Getter // Lombok annotation to generate getters for all fields (if any are added later)
@NoArgsConstructor // Lombok annotation to generate a no-argument constructor
public class HistoryEventsDto {
    // This class currently has no fields, but can be used as a placeholder or extended later
}
