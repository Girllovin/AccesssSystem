package smallITgroup.client.dto;

import lombok.Getter;
import lombok.NoArgsConstructor;

@Getter // Lombok annotation to generate getters for all fields (none currently defined)
@NoArgsConstructor // Lombok annotation to generate a no-argument constructor
public class ControllerDto {
    // Currently, this DTO (Data Transfer Object) is empty.
    // It can be used as a placeholder or base for future controller-related data.
}
